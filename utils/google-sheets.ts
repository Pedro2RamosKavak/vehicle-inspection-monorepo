// TODO: Implementar la integración con Google Sheets
// Esta es una implementación simulada para fines de demostración

export async function appendToGoogleSheet(data: any[]): Promise<boolean> {
  // Simular el envío de datos a Google Sheets
  return new Promise((resolve) => {
    setTimeout(() => {
      // Simular éxito
      resolve(true)
    }, 800)
  })
}
